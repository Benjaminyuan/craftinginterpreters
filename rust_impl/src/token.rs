#[derive(Clone, Debug)]
pub enum Token {
    LeftParen,
    RightParen,
    LeftBrace,
    RightBrace,
    Not,
    NotEqual,
    Comma,
    Dot,
    Minus,
    Plus,
    Semicolon,
    Star,
    EqualEqual,
    LessEqual,
    Less,
    Equal,
    GreaterEqual,
    Greater,
    Slash,
    Str(Literal),
    Number(Literal),
    Identifier(Literal),
    And,
    Class,
    Else,
    False,
    For,
    Fun,
    If,
    Nil,
    Or,
    Print,
    Return,
    Super,
    This,
    True,
    Var,
    While,
}
#[derive(Clone, Debug)]
pub enum Literal {
    Double(f64),
    Str(String),
}
